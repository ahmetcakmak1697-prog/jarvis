# DURUM — 2026-09-25 · nerede kaldık, sırada ne var

> **Bu belge TARİHLİ ve bilerek öyle.** `CLAUDE.md` §10'un dersi: sabit
> yazılmış "aktif cephe" ve commit hash'i altı hafta içinde eskidi ve her
> oturumu yanlış yönlendirdi. Buradaki her cümle 25 Eylül 2026 itibarıyla
> doğrudur; **canlı gerçek için** `git log`, `roadmap_state.json` ve
> `automation/HUMAN_NEEDED.md` okunur.

**Dal:** `auto/opencode-deepseek` · **Baş:** `66af7e1`

---

## 1. Geldiğimiz seviye — ne canlı, ölçülmüş haliyle

### Ses hattı ve akış (önceki oturumlar)

Mikrofon → faster-whisper → model → Edge TTS ucu uca çalışıyor.
`python main.py` ile açılır. Bulut modeli akıyor: ilk parça **1,24 sn**,
ilk cümle **1,53 sn** (ölçüldü). `JARVIS_STREAM=0` eski tek-parça
davranışa döner.

### Hafıza — bu oturumda kuruldu (11 commit)

JARVIS artık **hatırlıyor**, ve bu iddia değil ölçüm: soğuk süreçte,
soruyla kayıt arasında sıfır ortak kelime varken doğru cevabı verdi.

Kurulu parçalar:

| ne | nerede | durum |
|---|---|---|
| Anlamsal arama (okuma yolu) | `agent/anlamsal_hafiza.py` | canlı, eşik **0,45** ölçümle seçildi |
| Sohbet indeksi | `sohbet_indeksi` koleksiyonu | 34 parça · `jarvis_memories`'ten AYRI |
| İndeksleme + uzlaştırma | `scripts/hafiza_indeksle.py` | silinen kayıt indeksten de düşüyor |
| "Şunu unut" | `main.py` + `forget_by_keys` | onaysız silme YOK |
| Türkçe gömme modeli | `tools/vector_memory.py` | varsayılan; ölçüm 1/6 → 5/6 |

Yol boyunca bulunan ve düzeltilen üç sessiz kusur — üçü de "çalışıyor
görünüyordu":

1. **Akış modunda hiçbir tur kaydedilmiyordu.** 34 tur konuşuldu, diske
   sıfır satır düştü. JARVIS pratikte hafızasızdı; son-8-tur penceresi
   bile boştu. (`7aa07dc`)
2. **Chroma'nın varsayılan gömme modeli İngilizce.** Altı Türkçe sorguda
   1/6 — rastgele seçmenin beklentisi de 1/6. (`57c54c6`, `FAILURES.md`)
3. **Hafıza bloğunun kendi metni hafızayı susturuyordu.** "Bunlardan yeni
   olgu türetme" yazıyordu; model kaydı prompt'unda görüp "erişimim yok"
   diyordu. (`a5ad136`)

### Kapı (korunması gereken durum, hedef değil)

`pytest tests -q` **2126 geçti / 0 başarısız / 2 xfailed**, alfabetik
**ve** ters sırada. `ruff check .` **281** — oturum başındaki 283'ün
altında. Yeni test: 65.

### Airfel (ayrı depo, tamamlandı)

`ahmetcakmak1697-prog/airfel-klima-projesi` (private). İki klima 18/18
doğrulandı, panel canlı, `0xC1` grup 1 bayt haritası çıkarıldı.

---

## 2. Sırada ne var — üçü Ahmet'te, biri hazır

`roadmap_state.json` bu işleri **kapsamıyor**: 14 adımın 13'ü done, açık
tek adım `FAZ-3-E1` (proaktif davranış motoru) ve bu oturumun hafıza işi
kart güdümlüydü, yol haritası adımı değildi. Uydurma adım eklenmedi.
Gerçek sıra burada ve `HUMAN_NEEDED.md`'de.

### İlk iş — iki dakika, hükmü kapatır

`hotwords` için **tek bir gürültü klibi** eksik: müzik ya da TV normal
ses seviyesinde, Ahmet susarken, gürültü tabanının üstünde.

Neden bu kadar dar: kazanç zaten ölçüldü ve büyük (`small`/1 → 5/10 vs
`small`/5+`hotwords` → **9/10**), ama risk ölçülmedi. İki koşu yapıldı,
biri konuşma içerdiği için geçersiz, diğeri dijital sessizlik olduğu için
`vad_filter` her şeyi kesti. Kartın korktuğu durum **VAD'den geçen**
gürültü ve o hiç sınanmadı.

    .venv/Scripts/python.exe scripts/olc_stt_turkce.py dogrula-kaydet --okuma 0
    .venv/Scripts/python.exe scripts/olc_stt_turkce.py dogrula

Çıkmazsa hüküm **Geçti**'ye döner ve uygulama kartı yazılır. Çıkarsa
**Kaldı** olur: `hotwords` üretime alınmaz. Ayrıntı:
`automation/STT_DOGRULA_2026-09-25.md`.

**Not:** ölçüm `beam_size`'ın tek başına **zararlı** olduğunu gösterdi
(4/10 < 5/10). Yani beam 5'i `hotwords` olmadan almanın gerekçesi yok;
uygulama kartı ikisini birlikte ele almalı.

### İkinci — JARVIS'in yazı tipi

Ahmet "Tony Stark vizyonuna uygun, sade, şık, modern" dedi. Tıkanma
teknik değil: **lisanslanabilir bir yüz seçilmeli.** Seçim gelmeden
uygulanacak bir şey yok.

### Üçüncü — Airfel kWh tüketim modeli

Paneldeki kWh sayısı gerçek ölçüme dayanmıyor. Önerilen yol değişmedi:
**prizli wattmetre** ile gerçek tüketim ölçülsün, sonra model kurulsun.
Ölçülmemiş bir katsayı modeli yazmak, panelin geri kalanının disiplinini
bozar (panel kompresör tavanını 60 Hz varsaydığında gerçek tavan 71 Hz
çıkmıştı — aynı hata sınıfı).

### Dördüncü — kendiliğinden doğan aday

Bu oturumda ortaya çıktı: **tek bir konuşmayı unutmanın** yolu vardı ama
`temizle` dışında tek kayıt silen bir üretim yolu yoktu; elle SQL
yazılmıştı. O kapatıldı (`bf535af`). Bir sonraki doğal istek, unutmanın
sesli komuttan da çalışması — ama bu bir karta bağlanmadı, burada aday
olarak durur.

---

## 3. Bilerek ertelenenler — yazılı, ki taze fikir olarak geri gelmesin

- **`jarvis_memories`'in eski kayıtları** — koleksiyon boş olduğu için
  Türkçe modele geçiş bedelsiz oldu. Dolu olsaydı indeks baştan kurulmak
  zorundaydı; bu, bir sonraki gömme modeli değişikliğinin bedeli.
- **Kalıcı hafızaya otomatik yazma** — yok ve olmayacak (§7.1a). Onay
  hattı (`answer_crystallizer` → `knowledge_card_promoter`) baypas
  edilmedi; sohbet yolunda `jarvis_memories`'e yazan tek satır yok.
- **`hotwords` listesini büyütmek** — bu ölçüm aynı beş kelimeyi sınadı.
  Cihaz adlarıyla genişletmek ayrı bir ölçüm, ayrı bir kart.
- **Eşiğin yeniden ölçülmesi** — 0,45 sayısı 19 sondaj ve 39 parçalık bir
  indeksten geldi, **tek koşu**. İndeks büyüdükçe yeniden ölçülmeli;
  kalıbı `scripts/hafiza_indeksle.py --olc`.

---

## 4. Bu oturumun commit'leri

    66af7e1  docs(stt): dogrula measurement
    fa62ff4  feat(stt): dogrula probe
    bf535af  feat(memory): "şunu unut"
    47b4450  feat(memory): reconcile the index with SQLite
    00f8369  docs(kart): index reconciliation card
    a5ad136  fix(memory): three reasons it stayed silent
    7aa07dc  fix(memory): streamed turns were never saved
    57c54c6  fix(memory): jarvis_memories speaks Turkish
    8dfd30a  docs(memory): measurements + English-embedder trap
    d00a749  feat(memory): recall in both chat paths
    5412c85  feat(memory): semantic search over chat history
